server:
  port: 8088

spring:
  application:
    name: Syncpad Service

  datasource:
    url: jdbc:postgresql://localhost:5432/syncpad
    username: postgres
    password: ####
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

